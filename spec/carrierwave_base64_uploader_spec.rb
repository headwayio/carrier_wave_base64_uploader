require "spec_helper"

describe CarrierwaveBase64Uploader do
  it "has a version number" do
    expect(CarrierwaveBase64Uploader::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
